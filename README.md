# spring-boot-docker-compose

Explore and demonstrate how to use Spring Boot's docker-compose capabilities

## Notes

* No additional datasource configuration required (for local development) - the DataSource will be configured with
  the parameters used to set up the Docker container
